@model SportSpot.Entities.Models.User

<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center">
                <h2>@Model.Name's Profile</h2>
                <a asp-action="EditProfile" asp-route-id="@Model.Id" class="btn btn-primary">Edit Profile</a>
            </div>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <h4>Personal Information</h4>
                    <dl class="row">
                        <dt class="col-sm-4">Age:</dt>
                        <dd class="col-sm-8">@Model.Age</dd>

                        <dt class="col-sm-4">Height:</dt>
                        <dd class="col-sm-8">@Model.Height cm</dd>

                        <dt class="col-sm-4">Weight:</dt>
                        <dd class="col-sm-8">@Model.Weight kg</dd>

                        <dt class="col-sm-4">Preferred Sport:</dt>
                        <dd class="col-sm-8">@Model.Sports</dd>
                    </dl>
                </div>
                <div class="col-md-6">
                    <h4>Contact Information</h4>
                    <dl class="row">
                        <dt class="col-sm-4">Email:</dt>
                        <dd class="col-sm-8">@Model.Email</dd>

                        <dt class="col-sm-4">Phone:</dt>
                        <dd class="col-sm-8">@Model.Phone</dd>

                        <dt class="col-sm-4">Location:</dt>
                        <dd class="col-sm-8">@Model.City, @Model.Country</dd>

                        <dt class="col-sm-4">Address:</dt>
                        <dd class="col-sm-8">@Model.Address</dd>

                        <dt class="col-sm-4">Postal Code:</dt>
                        <dd class="col-sm-8">@Model.PostalCode</dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
<<<<<<< HEAD
</div>

<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <h3>Events Created</h3>
        </div>
        <div class="card-body">
            <div class="table-responsive" style="max-height: 300px; overflow-y: auto;">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Date</th>
                            <th>Sport</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (ViewBag.UserEvents != null && ((List<SportSpot.Entities.Models.Event>)ViewBag.UserEvents).Any())
                        {
                            foreach (var @eventItem in (List<SportSpot.Entities.Models.Event>)ViewBag.UserEvents)
                            {
                                <tr>
                                    <td>@eventItem.Title</td>
                                    <td>@eventItem.Date.ToString("yyyy-MM-dd")</td>
                                    <td>@eventItem.SportType</td>
                                    <td>
                                        @if ((bool) ViewBag.IsLoggedInUser)
                                                    {
                                <a href="/Event/EditEvent/@eventItem.Id" class="btn btn-warning btn-sm">Edit</a>
        }
                                                    else
                                                    {
                                <a href="/Event/EventDetails/@eventItem.Id" class="btn btn-primary btn-sm">View</a>
        }
                            </td>
                        </tr>
    }
}
                                    else
{
                        <tr>
                            <td colspan="4" class="text-center">No events to display.</td>
                        </tr>
}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
=======
>>>>>>> origin/master
</div>